import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.barcodelabel.designer',
  appName: 'Barcode Label Designer',
  webDir: 'dist'
};

export default config;
